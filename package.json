{
  "name": "alphaforge",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5173",
    "dev:host": "vite --host 0.0.0.0 --port 5173",
    "build": "vite build",
    "api": "uvicorn backend.main:app --host 127.0.0.1 --port 8000",
    "api:reload": "uvicorn backend.main:app --reload --host 127.0.0.1 --port 8000",
    "api:host": "uvicorn backend.main:app --host 0.0.0.0 --port 8000"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "lucide-react": "^0.475.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.1"
  }
}
